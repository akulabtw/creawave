name: 🚀 Ping CreaWave Bot

on:
  schedule:
    # Запускается каждые 5 минут
    - cron: '*/5 * * * *'
  workflow_dispatch:  # Позволяет запускать вручную

jobs:
  ping-bot:
    runs-on: ubuntu-latest
    
    steps:
    - name: 🔁 Ping the bot
      run: |
        echo "🕐 Время: $(date)"
        echo "📡 Отправляю запрос к боту..."
        
        # Отправляем GET запрос к вашему боту
        response_code=$(curl -s -o /dev/null -w "%{http_code}" "https://workspace.akulabtw.repl.co")
        
        if [ "$response_code" -eq 200 ]; then
          echo "✅ УСПЕХ! Бот активен. Код ответа: $response_code"
        elif [ "$response_code" -eq 000 ]; then
          echo "❌ ОШИБКА! Бот не отвечает. Проверьте URL."
        else
          echo "⚠️ ПРЕДУПРЕЖДЕНИЕ! Код ответа: $response_code"
        fi
        
        # Дополнительная проверка через ping
        echo "🔍 Дополнительная проверка..."
        curl -s "https://workspace.akulabtw.repl.co/ping" || echo "❌ Ping endpoint не доступен"
        
    - name: 📊 Логирование
      run: |
        echo "========================================"
        echo "🤖 CreaWave Bot Ping Service"
        echo "⏰ Следующий пинг через 5 минут"
        echo "========================================"
